# cs285_final_project

Minimal uv-friendly bootstrap for the CS285 final project worktree.

## Quickstart

Install dependencies and run the tiny training entry point:

```bash
uv run python scripts/train.py --config configs/base.yaml
```

The command loads the YAML config, seeds the process, and prints the experiment name.
